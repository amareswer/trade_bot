# trade_bot scheduled jobs — RETIRED 2026-07-14. Cron is no longer used.
#
# This file is kept (empty of jobs) as the record of why. Installing it
# clears the old broken schedule:
#   crontab ops/crontab.txt
# Verify (should show only comments):
#   crontab -l
#
# History — why cron was abandoned on this machine:
# - 2026-07-06: shadow_signal.py (daily 02:00) + live_comparison.py (Mon 09:00)
#   installed via this file.
# - 2026-07-10: moved to 12:05/12:10 after runs were missed overnight —
#   diagnosed then as lid-close sleep (cron never catches up missed runs).
# - 2026-07-14: mail spool (/var/mail) revealed the REAL failure: every cron
#   run since install — at 02:00 AND at 12:05 — died with
#   "/bin/sh: logs/shadow_signal.log: Operation not permitted".
#   macOS TCC denies /usr/sbin/cron access to ~/Desktop, where the repo
#   lives. Cron NEVER successfully ran either job; the two existing shadow
#   reports (Jul 6, Jul 11) were manual runs.
#
# Replacement: the crypto bot runs both audits itself — see
# _scheduled_audits_loop() in bot/main.py (daemon thread, fresh subprocess
# per run, catch-up after restart, failures in the bot log, state in
# logs/audit_state.json). Env knobs: AUDIT_SCHEDULER_ENABLED,
# SHADOW_AUDIT_TIME (default 12:05), WEEKLY_AUDIT_TIME (default 12:10).
#
# VPS migration: still prefer systemd timers (Persistent=true) over cron —
# or simply keep the in-bot scheduler, which works identically there.
