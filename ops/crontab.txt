# trade_bot scheduled jobs — SOURCE OF TRUTH for cron on any machine.
#
# Install / update (never edit the live crontab by hand):
#   crontab ops/crontab.txt
# Verify:
#   crontab -l
#
# Notes
# - Times are LOCAL machine time (this Mac runs ET; 12:05 ET ≈ 16:05 UTC in summer).
# - macOS cron only fires while the machine is awake and does NOT catch up on
#   missed runs. `caffeinate -i` prevents idle sleep but NOT lid-close sleep —
#   the 02:00 shadow job silently missed Jul 7–10, 2026 (lid closed overnight).
#   Jobs now run MIDDAY when the machine is reliably awake.
# - VPS migration: prefer systemd timers (Persistent=true catches missed runs,
#   failures land in journald). One .service + .timer per job below, installed
#   by deploy.sh. Until then, this file IS the schedule.

TRADE_BOT=/Users/nishita/Desktop/Amaresh/projects/trade_bot
PY=/Users/nishita/Desktop/Amaresh/projects/trade_bot/.venv/bin/python

# Shadow signal fidelity audit — daily. Confirms the live bot's candle-close
# decisions match a fresh replay of the validated strategy (capital-gate input).
# 12:05 local — moved from 02:00 (2026-07-10): machine asleep at 2am, runs missed.
5 12 * * *  cd $TRADE_BOT && $PY shadow_signal.py >> logs/shadow_signal.log 2>&1

# Live-vs-backtest comparison — weekly, Monday midday (roadmap item 17).
10 12 * * 1  cd $TRADE_BOT && $PY live_comparison.py >> logs/weekly.log 2>&1
